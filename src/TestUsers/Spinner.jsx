// Spinner.jsx
import React from "react";
import "./Spinner.css"; // Ensure you link the CSS file

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
